# Copy this file to .env.local and fill in real values for local testing.
# In production, set these as Environment Variables in the Vercel dashboard.

GMAIL_USER=bookings@yourdomain.com
GMAIL_APP_PASSWORD=
# Multiple recipients allowed — comma-separated, e.g. "a@x.com, b@y.com"
NOTIFY_TO=bookings@yourdomain.com
